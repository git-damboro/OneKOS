# 千面·OneKOS 工作手册

> 用途：这是项目的长期状态页和执行手册。每完成一个任务，更新「最后更新」「状态」「证据链接/命令」三项；不在本文记录 App Secret、模型 Key、访问 Token 或用户隐私数据。

**最后更新：** 2026-08-04  
**当前代码分支：** `feat/feishu-poc`  
**统一数据底座：** 飞书多维表格「千面·OneKOS 运营中台（演示）」  
**MVP 数据原则：** 顾问、内容、抖音互动、线索与 CRM 数据均须显式标注 `模拟数据=是`；品牌事实只可引用有效、非模拟且未过期的知识记录。

---

## 1. 一句话现状

OneKOS Web 工作台、外部大模型生成、飞书 Base 数据读写与「内容—评论—线索—人工确认学习」服务端闭环已经完成；**当前不能把 Aily 当作 Web 可调用的 API**，也不需要这样做。Aily 的正确角色是飞书内的轻量入口，Web 的正确角色是顾问的完整工作台，二者共享同一套 Base 数据和安全规则。

```text
顾问 Web 工作台 ──> OneKOS Node 服务 ──> 飞书 Base
                  └────────────────────> DeepSeek（内容/分析）

飞书内 Aily ──> OneKOS 多维表格桥接技能 ──> 飞书 Base

抖音数据（待接入） ──> 标准化互动数据 ──> 评论线索表 / 反馈事件表
CRM / 试驾（后续授权） ──> 结果回传 ──> 反馈事件表
```

---

## 2. 当前完成度

| 模块 | 状态 | 已完成内容 | 仍需处理 |
|---|---|---|---|
| OneKOS Web 工作台 | 已完成 | 顾问任务、动态画像、内容包、四重质检、评论分析、线索与人工确认页面 | 演示前做一次完整回归与数据复位 |
| OneKOS 服务端 | 已完成 | API、幂等写回、事实/合规/人设/矩阵质检、人工接管门禁 | 保持测试通过，不做无关重构 |
| DeepSeek | 已接通 | 通过 OpenAI-compatible 接口生成内容与分析评论 | 仅保存在本机 `.env`；演示前确认余额、网络与超时设置 |
| 飞书 Base（Web 服务端） | 已接通 | 自建应用、Bitable API、内容/线索/反馈读写及 Base 表结构 | 核对应用协作者权限长期有效 |
| 品牌知识库 | 初版完成 | 已录入乐道 L60 的 10 条有效、非模拟官方事实 | 持续补充车型、权益、门店/活动等有来源且有有效期的信息 |
| Aily 智能体 | 可用但未验收 | 已创建「千面·OneKOS 内容搭档」，可在飞书内对话 | 不开放 OpenAPI channel，不能给 Web 提供 API；无需继续排查该限制 |
| Aily Base 桥接技能 | 阻塞 | 已安装唯一技能 `onekos-bitable-bridge-v2` | 现有运行结果仍为 `source_mode=local`，必须替换为真实 Base 只读包 |
| 抖音数据 | 待同事接入 | 已确定评论分析、点赞/收藏/转发等指标的业务用途 | 等待同事交付标准数据结构与模拟/真实数据标记 |
| CRM / 试驾 | MVP 暂不接入 | 已在流程中保留 A 级线索人工接管与结果回传位置 | 需要企业授权、字段映射和隐私评估后再实施 |

---

## 3. 关键判断：当前还差什么

### 3.1 不需要再做的事

| 事项 | 结论 | 原因 |
|---|---|---|
| 让 Web 调用 Aily API | 不做 | 当前租户返回 `openapi channel is not enabled for this agent`；Web 已可直接调用 DeepSeek。 |
| 为 MVP 部署 HTTPS | 暂不做 | 本地 Web 演示 + 飞书内 Aily + 共享 Base 已能证明完整逻辑。 |
| 接入真实抖音账号后再演示 | 暂不等待 | 当前可用贴近业务的模拟互动数据演示，真实接入单独迭代。 |
| 自动私信、加微信、预约试驾、自动发布 | 不做 | 高风险销售动作必须顾问人工确认。 |

### 3.2 MVP 上线演示前必须完成（P0）

| 优先级 | 问题 | 完成标准 | 负责人建议 |
|---|---|---|---|
| P0 | Aily V2 技能仍读本地 assets | 调用三个只读方法均返回 `source_mode=feishu_base`，且数据为成都/ADV-017/TASK-001/Base 中真实记录 | 本项目 / 技能维护人 |
| P0 | 明确 Aily 与 Web 的分工 | Aily 对话展示「读画像、读内容、读线索」；Web 展示「生成、质检、人工确认、写回」 | 演示负责人 |
| P0 | 演示环境检查 | `npm test`、`npm run smoke`、`npm run check` 全部通过；本机服务可访问 | 演示负责人 |
| P0 | 数据真实性标识 | 页面、Base 记录、Aily 输出中均能看到模拟数据标记；品牌事实不标模拟 | 数据负责人 |

### 3.3 复赛方案增强项（P1）

| 优先级 | 事项 | 交付物 / 验收标准 |
|---|---|---|
| P1 | 抖音数据接入 | 将评论、点赞、收藏、转发、视频表现按数据契约写入中间表或接口；支持 `is_simulation`、采集时间、内容 ID、账号 ID、平台字段。 |
| P1 | 品牌知识治理 | 建立「来源、核验日期、有效期至、状态、适用车型」五项必填；价格与权益必须设置失效日期。 |
| P1 | 试点指标看板 | 可查看内容准备时间、轻改采用率、近重复率、合格线索/千次曝光四项指标。 |
| P1 | 演示视频 | 录制 3–5 分钟从顾问任务到 A 级线索人工接管的完整链路。 |

### 3.4 正式试点后再做（P2）

| 事项 | 前置条件 |
|---|---|
| CRM / 试驾系统接入 | 企业书面授权、字段映射、客户隐私与最小权限方案。 |
| 真实抖音自动采集 | 取得平台/企业允许的官方数据通道与账号授权。 |
| Aily 调用 OneKOS Web 服务 | 有可部署 HTTPS 地址、鉴权方案和开放 API 渠道后再评估。 |
| 多门店规模化 | 试点通过采用率、同质化率、线索质量三类指标验证。 |

---

## 4. Aily 桥接技能修复手册（当前第一优先级）

### 4.1 目标

替换当前会返回「合肥宝爸 / BRAND-L60-* / source_mode=local」的技能。Aily 只允许读取本项目的飞书 Base，不得静默读取本地演示 assets，不得产生写操作。

### 4.2 目标技能约束

| 项目 | 必须要求 |
|---|---|
| 技能名称 | `onekos-bitable-bridge-live`（建议新包；避免与 V2 旧缓存混淆） |
| 数据源 | 默认飞书 Base；仅当调用方显式传 `demo=true` 时可读取 assets |
| Base | `G3W9bPQLzaGD5zs5zz1cVh3znme` |
| CLI 身份 | 全部读取使用 `lark-cli base --as bot` |
| 允许操作 | `get_advisor_context`、`get_content_package`、`get_lead_context` |
| 禁止操作 | 写表、发布、私信、加微信、预约试驾、价格承诺、修改画像权重 |
| 失败策略 | 远程读取失败明确返回 `ok:false` 与错误信息；不得回退到 assets |

### 4.3 安装后的验收指令

在 Aily 对话中逐条执行；任一结果出现 `local`、`合肥`、`宝爸` 或 `BRAND-L60-*`，均视为不合格。

```text
使用 OneKOS 多维表格桥接器 LIVE，读取 ADV-017 和 TASK-001。
只读返回顾问城市、有效画像标签、用户问题、内容角度与 source_mode。
```

**通过标准：** 城市为「成都」，内容角度含「成都工作日晚高峰补能路线」，`source_mode=feishu_base`。

```text
使用 OneKOS 多维表格桥接器 LIVE，调用 get_content_package，读取 CONTENT-DEMO-001。
只读返回标题、开场、状态、任务ID、四重质检分、模拟数据与 source_mode。
```

**通过标准：** `source_mode=feishu_base`，且只返回 Base 中的 CONTENT-DEMO-001；不自动发布、不写回。

```text
使用 OneKOS 多维表格桥接器 LIVE，按 COMMENT-DEMO-001 调用 get_lead_context。
只读返回线索等级、线索分、城市、家庭结构、车型、购车时间、试驾意愿、下一步动作、是否人工接管与 source_mode。
```

**通过标准：** `source_mode=feishu_base`；A 级线索固定为 `human_handoff_required=true`、`next_action=人工接管`。

### 4.4 需要保留的事实边界

- Aily 没有公开 API 是平台能力限制，不是本项目代码故障。
- Aily 的价值是飞书内低负担入口，而不是替代 OneKOS Web 工作台。
- 内容生成仍在 Web 服务中通过 DeepSeek 完成；Aily 可展示/读取同一份结果。
- 一切真实客户动作均由顾问人工执行。

---

## 5. 抖音数据接入工作手册（交给同事）

### 5.1 对接目标

提供一个**不直接触发任何用户动作**的数据输入层：把每条视频及其评论互动标准化后交给 OneKOS。OneKOS 负责风向总结、内容选题反馈和线索分级；抖音侧只负责在授权范围内提供数据。

### 5.2 最小数据契约

| 数据对象 | 必填字段 | 作用 |
|---|---|---|
| 视频表现 | `platform`、`content_id`、`video_id`、`published_at`、`view_count`、`like_count`、`favorite_count`、`share_count`、`is_simulation`、`collected_at` | 评估选题与内容表现，不单独判定销售线索。 |
| 评论 | `platform`、`comment_id`、`content_id`、`comment_text`、`commented_at`、`like_count`、`is_simulation`、`collected_at` | 提取城市、家庭、车型、购车时间、试驾意愿与原文证据。 |
| 评论回复（可选） | `comment_id`、`reply_text`、`replied_at`、`operator_type` | 只用于评估人工回复效果；不得让系统自动发送。 |
| 账号映射 | `platform_account_id`、`advisor_id`、`authorization_status` | 将互动准确归属到顾问，避免跨账号学习。 |

### 5.3 数据质量与安全要求

- 所有测试样本必须 `is_simulation=true`；真实数据必须有账号授权和采集来源。
- 不采集或展示用户手机号、微信号、身份证等不必要个人信息。
- 每个字段必须保留 `collected_at`，评论解析结果必须保留原文证据。
- 数据接入仅「读取/导入」，不允许借由接口自动点赞、回复、关注或私信。
- A 级线索只写入「待顾问人工接管」，不得自动预约或触达。

---

## 6. 日常演示工作流

| 阶段 | 顾问可见动作 | 系统动作 | 人工门禁 |
|---|---|---|---|
| 1. 冷启动 | 补充少量经历、语音或历史内容并确认 | 形成带权重、置信度、来源与证据的动态画像标签 | 顾问确认后画像才进入学习 |
| 2. 每日任务 | 查看 1–3 个任务 | 根据品牌任务、用户问题、地域场景、标签权重和矩阵空白路由 | 顾问选择是否执行 |
| 3. 内容生成 | 补充真实素材、轻改、确认 | DeepSeek 生成选题、脚本、分镜、素材、标题、评论预案；服务端四重质检 | 事实缺失则留白；不得自动发布 |
| 4. 互动回流 | 查看评论风向与建议 | 解析评论字段、生成线索等级和下一步建议 | A 级线索必须人工接管 |
| 5. 策略学习 | 选择采用/轻改/拒绝，并记录跟进结果 | 创建反馈事件，输出建议权重变化 | 只有顾问确认后才更新画像权重 |

---

## 7. 演示前检查清单

### 7.1 本机 Web 服务

```powershell
cd E:\Desktop\BISAI\OneKOS_MVP演示\.worktrees\feishu-poc
npm test
npm run smoke
npm run check
npm start
```

打开 `http://127.0.0.1:4173/`，确认「飞书落地中心」显示当前真实运行模式；绝不在页面、截图或录屏中展示 `.env` 内容。

### 7.2 飞书 Base

- 自建应用仍是 Base 协作者，且已获得所需的记录读写权限。
- 顾问、任务、评论、线索与反馈样本均保留「模拟数据」标记。
- 品牌知识记录具有来源、来源 URL、核验日期、有效期、状态和模拟数据标记。
- 演示前不删除已有记录；如需重置，仅新增新的演示 ID，避免误覆盖。

### 7.3 演示叙事

1. 先展示动态标签画像，而不是给顾问贴固定人格标签。
2. 展示「一题千解」如何避开矩阵回声室。
3. 展示事实核与人格壳分离：品牌事实来自知识库，表达来自顾问画像与真实素材。
4. 展示评论到 A 级线索，但强调只给建议、必须顾问接管。
5. 最后展示 Base 与 Aily：同一数据底座、不同低负担入口。

---

## 8. 维护日志模板

每次有重要修改，在本节顶部新增一行：

| 日期 | 修改人 | 改动 | 状态 | 证据 |
|---|---|---|---|---|
| 2026-08-04 | 项目组 | 建立项目工作手册；明确 Web/DeepSeek/Base 已接通，Aily LIVE 技能为 P0 阻塞项 | 进行中 | 当前分支 `feat/feishu-poc` |

